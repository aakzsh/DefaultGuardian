import 'package:flutter/material.dart';

class ColorPalette {
  static const blue = Color.fromRGBO(9, 177, 242, 1);
  static const bg = Color.fromRGBO(18, 18, 18, 1);
  static const secbg = Color.fromRGBO(28, 29, 29, 1);
  static const sliderclr = Color.fromRGBO(154, 154, 154, 1);
}
